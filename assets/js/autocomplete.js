// let items = document.getElementById('items').children;
// document.getElementById('myInput') onkeyup function(){
// 	let searchterm = this.value;
// 	console.log(searchterm);
// 	for (i=0; i < items.length; i++){
// 		if(items[i].innerText.indexOf(searchterm)>-1){
// 			items[i].style.display = 'block';
// 		}
// 		else{
// 			items[i].style.display = 'none';
// 		};
// 	};
// };

/* we see what the user typed in the console*/
/* for starts a loop */
/* i starts at first item, 0, and counts up through the list of items */
/* -1 means the letter/string does not exist in the search. 0, 1, 2 would be the index number of the <li>s */
/* if it is in the search, display it. if it doesn't, hide it. */
/* Find a algorithm for common mispellings */
/* There may be a free one for JUST THIS SITE */